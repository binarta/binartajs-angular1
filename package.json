{
  "scripts": {
    "build": "gulp",
    "test": "karma start karma.conf.js --single-run --browsers ChromeHeadless",
    "test:phantomjs": "karma start karma10.conf.js --single-run --browsers PhantomJS",
    "test:watch": "karma start karma.conf.js",
    "test:rest": "karma start karma-rest.conf.js --single-run --browsers ChromeHeadless",
    "test:rest:watch": "karma start karma-rest.conf.js",
    "templates": "npm run templates:all",
    "templates:all": "gulp",
    "templates:watch":"gulp templates:watch",
    "templates:default": "gulp templates-bootstrap3",
    "templates:shop": "gulp templates-shop-bootstrap3",
    "templates:publisher": "gulp publisher-bootstrap3",
    "postinstall": "bower install"
  },
  "devDependencies": {
    "gulp": "^3.8.10",
    "gulp-angular-templatecache": "^1.5.0",
    "gulp-minify-html": "^0.1.8",
    "gulp-template": "^3.0.0",
    "jasmine-core": "^2.4.1",
    "karma": "^0.13.15",
    "karma-chrome-launcher": "^2.2.0",
    "karma-jasmine": "^0.3.6",
    "karma-junit-reporter": "^0.4.0",
    "karma-phantomjs-launcher": "^0.2.1",
    "phantomjs": "^2.1.1"
  }
}
